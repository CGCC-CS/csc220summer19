#ifndef MYFILE_H  /* Check if myfile.h has already been included */
#define MYFILE_H  /* If it hasn't been defined, define it */

/* Symbolic constants */
#define FAVORITE 42 

/* Forward Declarations */
void init(void);
int addone(int n);
void doubleIt(int *n);
int factorial(int n);

#endif
